# AI Agent Workflows
AI Agents use tools to solve complex tasks autonomously.
Workflows can be Sequential (Step 1 -> Step 2), Parallel (do tasks simultaneously), or Conditional (if X, then Y).
LangGraph allows developers to build stateful, multi-agent systems.