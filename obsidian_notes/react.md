# React JS
React is a JavaScript library for building user interfaces.
It uses a component-based architecture where the UI is broken down into reusable pieces.
Key concepts include Props (passing data down), State (dynamic data), and Hooks like `useState` and `useEffect`.